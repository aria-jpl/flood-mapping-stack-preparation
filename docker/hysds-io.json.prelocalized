{
  "label": "(PRELOCALIZED, BROKEN) Prepare stack for ARIARNN flood-mapping",
  "submission_type": "individual",
  "enable_dedup": false,
  "params": [
      {
        "name": "localize_url",
        "from": "dataset_jpath:_source",
        "lambda": "lambda ds: filter(lambda x: x.startswith('s3://'), ds['urls'])[0]"
      }
  ]
}
